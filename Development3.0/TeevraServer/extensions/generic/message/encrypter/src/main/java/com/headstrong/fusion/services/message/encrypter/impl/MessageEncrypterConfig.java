package com.headstrong.fusion.services.message.encrypter.impl;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import com.headstrong.fusion.services.message.encrypter.impl.MessageEncrypterConfig;

public class MessageEncrypterConfig {
	private static final Logger logger = LoggerFactory
			.getLogger(MessageEncrypterConfig.class);
}
